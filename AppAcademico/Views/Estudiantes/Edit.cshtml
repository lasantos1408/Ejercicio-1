@model AppAcademico.Models.Estudiante

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Editar</h1>

<h4>Estudiante</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdEstudiante" />
            <div class="form-group">
                <label asp-for="Nombre" class="control-label"></label>
                <input asp-for="Nombre" class="form-control" />
                <span asp-validation-for="Nombre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Usuario Estudiante</label>
                <input asp-for="usuarioEst" class="form-control" />
                <span asp-validation-for="usuarioEst" class="text-danger"></span>
            </div>
            <div class="form-group">

                Curso
                @Html.DropDownListFor(m => m.IdCurso, (IEnumerable<SelectListItem>)ViewBag.cursoLista, "Seleccione el curso", new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.IdCurso, "", new { @class = "text-danger" })
                @*<label asp-for="IdCurso" class="control-label"></label>
        <input asp-for="IdCurso" class="form-control" />
        <span asp-validation-for="IdCurso" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Regresar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
